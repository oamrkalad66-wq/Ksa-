-- =======================================================
-- ⚡ VOID VIP - MASS SERVER HOP (ORIGINAL UI + BAKERY COORDS) ⚡
-- =======================================================

local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local LocalPlayer = Players.LocalPlayer

local queueOnTeleport = queue_on_teleport or (syn and syn.queue_on_teleport) or (fluxus and fluxus.queue_on_teleport) or queueonteleport

if queueOnTeleport then
    pcall(function()
        queueOnTeleport([[
            task.wait(1.5)
            loadstring(game:HttpGet("https://raw.githubusercontent.com/mdltt/VoidVIP/main/main.lua"))()
        ]])
    end)
end

local CoreGui = game:GetService("CoreGui")
local ParentGui = (gethui and gethui()) or CoreGui:FindFirstChild("RobloxGui") or LocalPlayer:WaitForChild("PlayerGui")
if ParentGui:FindFirstChild("VoidVIP_VioletCyan") then
    ParentGui.VoidVIP_VioletCyan:Destroy()
end

---------------------------------------------------------
-- 1. المحرك الصوتي (SFX Engine)
---------------------------------------------------------
local SFX = {
    Open = "rbxassetid://9114223178",
    Close = "rbxassetid://6029871317",
    Click = "rbxassetid://6895079853",
    Teleport = "rbxassetid://9114221160",
    Notify = "rbxassetid://4590662766"
}

local function PlaySFX(soundId, pitch, vol)
    task.spawn(function()
        local sound = Instance.new("Sound")
        sound.SoundId = soundId
        sound.Volume = vol or 0.8
        sound.PlaybackSpeed = pitch or 1
        sound.Parent = SoundService
        sound:Play()
        sound.Ended:Connect(function() sound:Destroy() end)
    end)
end

---------------------------------------------------------
-- 2. نظام الأخذ الفوري التام (Prompt 0.0s)
---------------------------------------------------------
local function optimizePrompt(prompt)
    if prompt:IsA("ProximityPrompt") then
        prompt.HoldDuration = 0
        prompt.RequiresLineOfSight = false
        prompt.MaxActivationDistance = 30
    end
end

for _, obj in ipairs(workspace:GetDescendants()) do optimizePrompt(obj) end
workspace.DescendantAdded:Connect(optimizePrompt)

---------------------------------------------------------
-- 3. قائمة المواقع والأزرار (موقع البيك الجديد فقط)
---------------------------------------------------------
local Locations = {
    {name = "🛒 شراء ذكي 6k", type = "SmartBuy"},
    {name = "🍗 2. مطعم البيك", cframe = CFrame.new(-949.7, 3.7, -292.2)},
    {name = "🪧 3. مقهى ساين", cframe = CFrame.new(-473.153, 5.553, -290.384)},
    {name = "☕ 4. هاف مليون", cframe = CFrame.new(-353.155, 5.158, -279.111)},
    {name = "🛒 5. بقالة الأولى", cframe = CFrame.new(506.612, 9.414, -311.789)},
    {name = "🏪 6. بقالة الثانية", cframe = CFrame.new(596.610, 6.023, -341.897)},
    {name = "🛍️ 7. بقالة الثالثة", cframe = CFrame.new(1296.671, 5.560, -220.331)},
    {name = "🍔 8. مطعم كودو", cframe = CFrame.new(-95.566, 6.149, 547.661)},
    {name = "🍗 9. مطعم الطازج", cframe = CFrame.new(-1742.8, 11.0, -32331.7)}
}

---------------------------------------------------------
-- 4. إعداد الشاشة والإشعارات
---------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "VoidVIP_VioletCyan"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = ParentGui

local function Notify(title, msg)
    PlaySFX(SFX.Notify, 1.1, 0.85)
    
    local Toast = Instance.new("Frame")
    Toast.Size = UDim2.new(0, 280, 0, 68)
    Toast.Position = UDim2.new(1, 40, 0.72, 0)
    Toast.BackgroundColor3 = Color3.fromRGB(12, 10, 22)
    Toast.BorderSizePixel = 0
    Toast.ClipsDescendants = true
    Toast.Parent = ScreenGui
    
    local ToastCorner = Instance.new("UICorner", Toast)
    ToastCorner.CornerRadius = UDim.new(0, 12)
    
    local ToastStroke = Instance.new("UIStroke", Toast)
    ToastStroke.Color = Color3.fromRGB(0, 160, 210)
    ToastStroke.Thickness = 1.6
    
    local TTitle = Instance.new("TextLabel", Toast)
    TTitle.Size = UDim2.new(1, -20, 0, 24)
    TTitle.Position = UDim2.new(0, 10, 0, 6)
    TTitle.BackgroundTransparency = 1
    TTitle.Text = title
    TTitle.TextColor3 = Color3.fromRGB(170, 90, 255)
    TTitle.Font = Enum.Font.GothamBold
    TTitle.TextSize = 13.5
    TTitle.TextXAlignment = Enum.TextXAlignment.Right
    
    local TMsg = Instance.new("TextLabel", Toast)
    TMsg.Size = UDim2.new(1, -20, 0, 24)
    TMsg.Position = UDim2.new(0, 10, 0, 30)
    TMsg.BackgroundTransparency = 1
    TMsg.Text = msg
    TMsg.TextColor3 = Color3.fromRGB(220, 235, 245)
    TMsg.Font = Enum.Font.GothamMedium
    TMsg.TextSize = 12
    TMsg.TextXAlignment = Enum.TextXAlignment.Right
    
    local ProgressBar = Instance.new("Frame", Toast)
    ProgressBar.Size = UDim2.new(1, 0, 0, 3)
    ProgressBar.Position = UDim2.new(0, 0, 1, -3)
    ProgressBar.BackgroundColor3 = Color3.fromRGB(0, 180, 230)
    ProgressBar.BorderSizePixel = 0
    
    TweenService:Create(Toast, TweenInfo.new(0.45, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
        Position = UDim2.new(1, -295, 0.72, 0)
    }):Play()
    
    TweenService:Create(ProgressBar, TweenInfo.new(2.8, Enum.EasingStyle.Linear), {
        Size = UDim2.new(0, 0, 0, 3)
    }):Play()
    
    task.delay(2.9, function()
        local exit = TweenService:Create(Toast, TweenInfo.new(0.35, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
            Position = UDim2.new(1, 40, 0.72, 0)
        })
        exit:Play()
        exit.Completed:Connect(function() Toast:Destroy() end)
    end)
end

---------------------------------------------------------
-- 5. الإطار الرئيسي
---------------------------------------------------------
local Main = Instance.new("Frame", ScreenGui)
Main.Name = "MainFrame"
Main.Size = UDim2.new(0, 240, 0, 360)
Main.Position = UDim2.new(0.08, 0, 0.2, 0)
Main.BackgroundColor3 = Color3.fromRGB(10, 8, 18)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true
Main.ClipsDescendants = true

local MainCorner = Instance.new("UICorner", Main)
MainCorner.CornerRadius = UDim.new(0, 16)

local MainStroke = Instance.new("UIStroke", Main)
MainStroke.Color = Color3.fromRGB(140, 60, 230)
MainStroke.Thickness = 1.8

local Header = Instance.new("Frame", Main)
Header.Size = UDim2.new(1, 0, 0, 44)
Header.BackgroundColor3 = Color3.fromRGB(18, 12, 32)
Header.BorderSizePixel = 0

local HeaderCorner = Instance.new("UICorner", Header)
HeaderCorner.CornerRadius = UDim.new(0, 16)

local TitleText = Instance.new("TextLabel", Header)
TitleText.Size = UDim2.new(1, 0, 1, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = "⚡ VOID VIP ⚡"
TitleText.TextColor3 = Color3.fromRGB(0, 190, 230)
TitleText.Font = Enum.Font.GothamBold
TitleText.TextSize = 14

local Scroll = Instance.new("ScrollingFrame", Main)
Scroll.Size = UDim2.new(1, -14, 1, -98)
Scroll.Position = UDim2.new(0, 7, 0, 50)
Scroll.BackgroundTransparency = 1
Scroll.BorderSizePixel = 0
Scroll.ScrollBarThickness = 3
Scroll.ScrollBarImageColor3 = Color3.fromRGB(0, 160, 210)

local ListLayout = Instance.new("UIListLayout", Scroll)
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ListLayout.Padding = UDim.new(0, 6)

ListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
    Scroll.CanvasSize = UDim2.new(0, 0, 0, ListLayout.AbsoluteContentSize.Y + 8)
end)

for _, loc in ipairs(Locations) do
    local Btn = Instance.new("TextButton", Scroll)
    Btn.Size = UDim2.new(1, -6, 0, 32)
    Btn.BackgroundColor3 = Color3.fromRGB(18, 14, 30)
    Btn.Text = loc.name
    Btn.TextColor3 = Color3.fromRGB(220, 230, 245)
    Btn.Font = Enum.Font.GothamMedium
    Btn.TextSize = 11.5
    Btn.TextXAlignment = Enum.TextXAlignment.Right
    
    local Padding = Instance.new("UIPadding", Btn)
    Padding.PaddingRight = UDim.new(0, 10)
    
    local BtnCorner = Instance.new("UICorner", Btn)
    BtnCorner.CornerRadius = UDim.new(0, 8)
    
    local BtnStroke = Instance.new("UIStroke", Btn)
    BtnStroke.Color = Color3.fromRGB(35, 25, 55)
    BtnStroke.Thickness = 1
    
    Btn.MouseEnter:Connect(function()
        TweenService:Create(Btn, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            BackgroundColor3 = Color3.fromRGB(0, 130, 180),
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }):Play()
        TweenService:Create(BtnStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(160, 70, 240)}):Play()
    end)
    
    Btn.MouseLeave:Connect(function()
        TweenService:Create(Btn, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            BackgroundColor3 = Color3.fromRGB(18, 14, 30),
            TextColor3 = Color3.fromRGB(220, 230, 245)
        }):Play()
        TweenService:Create(BtnStroke, TweenInfo.new(0.2), {Color = Color3.fromRGB(35, 25, 55)}):Play()
    end)
    
    Btn.MouseButton1Click:Connect(function()
        PlaySFX(SFX.Click, 1.1, 0.9)
        
        TweenService:Create(Btn, TweenInfo.new(0.08), {Size = UDim2.new(0.94, 0, 0, 28)}):Play()
        task.wait(0.08)
        TweenService:Create(Btn, TweenInfo.new(0.15, Enum.EasingStyle.Back), {Size = UDim2.new(1, -6, 0, 32)}):Play()
        
        if loc.type == "SmartBuy" then
            local args = {
                [1] = "Buy",
                [2] = {
                    ["\217\129\216\167\216\170\216\173 \216\167\217\130\217\129\216\167\217\132 \217\133\216\170\217\130\216\175\217\133"] = 3
                },
                [3] = 6000
            }
            local success, err = pcall(function()
                game:GetService("ReplicatedStorage").Replicated.Events.MarketEvent.Event:InvokeServer(unpack(args))
            end)
            if success then
                Notify("تم الشراء بنجاح 🛒", "تم تنفيذ الطلب بقيمة 6000")
            else
                Notify("خطأ في الشراء ❌", "تأكد من الحدث أو المكان")
            end
        else
            local char = LocalPlayer.Character
            if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.CFrame = loc.cframe
                Notify("تم الانتقال بنجاح ⚡", loc.name)
            end
        end
    end)
end

---------------------------------------------------------
-- زر تغيير السيرفر (بحث شامل في آلاف السيرفرات وإظهار العدد)
---------------------------------------------------------
local HopBtn = Instance.new("TextButton", Main)
HopBtn.Size = UDim2.new(1, -14, 0, 36)
HopBtn.Position = UDim2.new(0, 7, 1, -42)
HopBtn.BackgroundColor3 = Color3.fromRGB(150, 25, 80)
HopBtn.Text = "🌐 تغيير السيرفر (Server Hop)"
HopBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
HopBtn.Font = Enum.Font.GothamBold
HopBtn.TextSize = 11.5

local HopCorner = Instance.new("UICorner", HopBtn)
HopCorner.CornerRadius = UDim.new(0, 9)

if not getgenv().VisitedServers then
    getgenv().VisitedServers = {}
end

HopBtn.MouseButton1Click:Connect(function()
    PlaySFX(SFX.Click, 1, 0.9)
    HopBtn.Text = "⏳ جاري البحث في آلاف السيرفرات..."
    Notify("البحث الشامل", "جاري فحص السيرفرات المتاحة...")
    
    task.spawn(function()
        local currentJobId = game.JobId
        table.insert(getgenv().VisitedServers, currentJobId)
        
        local allServers = {}
        local cursor = ""
        local maxPages = 15
        local pagesFetched = 0
        
        repeat
            pagesFetched = pagesFetched + 1
            local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100" .. (cursor ~= "" and "&cursor=" .. cursor or "")
            
            local success, response = pcall(function()
                return game:HttpGet(url)
            end)
            
            if success and response then
                local data = HttpService:JSONDecode(response)
                if data and data.data then
                    for _, server in ipairs(data.data) do
                        local isVisited = false
                        for _, vId in ipairs(getgenv().VisitedServers) do
                            if vId == server.id then
                                isVisited = true
                                break
                            end
                        end
                        
                        if not isVisited and server.playing < server.maxPlayers then
                            table.insert(allServers, server.id)
                        end
                    end
                    cursor = data.nextPageCursor
                else
                    break
                end
            else
                break
            end
            task.wait(0.05)
        until not cursor or pagesFetched >= maxPages
        
        if #allServers > 0 then
            HopBtn.Text = "⚡ لقى " .. #allServers .. " سيرفر!"
            local randomServer = allServers[math.random(1, #allServers)]
            table.insert(getgenv().VisitedServers, randomServer)
            
            if queueOnTeleport then
                pcall(function()
                    queueOnTeleport([[
                        task.wait(1.5)
                        loadstring(game:HttpGet("https://raw.githubusercontent.com/mdltt/VoidVIP/main/main.lua"))()
                    ]])
                end)
            end
            
            TeleportService:TeleportToPlaceInstance(game.PlaceId, randomServer, LocalPlayer)
        else
            HopBtn.Text = "🔄 إعادة تعيين القائمة..."
            getgenv().VisitedServers = {currentJobId}
            task.wait(1)
            HopBtn.Text = "🌐 تغيير السيرفر (Server Hop)"
            Notify("تنبيه", "تمت زيارة السيرفرات، جرب مرة أخرى الآن!")
        end
    end)
end)

---------------------------------------------------------
-- 6. زر V المائي
---------------------------------------------------------
local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Name = "VoidToggleBtn"
ToggleBtn.Size = UDim2.new(0, 52, 0, 52)
ToggleBtn.Position = UDim2.new(0.02, 0, 0.4, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(12, 10, 22)
ToggleBtn.Text = "V"
ToggleBtn.TextColor3 = Color3.fromRGB(0, 190, 230)
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.TextSize = 25
ToggleBtn.Active = true
ToggleBtn.Draggable = true
ToggleBtn.Rotation = 0

local ToggleCorner = Instance.new("UICorner", ToggleBtn)
ToggleCorner.CornerRadius = UDim.new(1, 0)

local ToggleStroke = Instance.new("UIStroke", ToggleBtn)
ToggleStroke.Color = Color3.fromRGB(150, 60, 230)
ToggleStroke.Thickness = 2.2

local isCoolDown = false
ToggleBtn.MouseButton1Click:Connect(function()
    if isCoolDown then return end
    isCoolDown = true
    ToggleBtn.Rotation = 0
    
    local spinTween = TweenService:Create(ToggleBtn, TweenInfo.new(0.45, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
        Rotation = 360,
        Size = UDim2.new(0, 60, 0, 60)
    })
    spinTween:Play()
    spinTween.Completed:Connect(function() ToggleBtn.Rotation = 0 end)
    
    task.delay(0.2, function()
        TweenService:Create(ToggleBtn, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 52, 0, 52)
        }):Play()
    end)
    
    if Main.Visible then
        PlaySFX(SFX.Close, 1.25, 0.8)
        local tweenOut = TweenService:Create(Main, TweenInfo.new(0.22, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
            Size = UDim2.new(0, 0, 0, 0)
        })
        tweenOut:Play()
        tweenOut.Completed:Connect(function()
            Main.Visible = false
            isCoolDown = false
        end)
    else
        PlaySFX(SFX.Open, 1, 0.85)
        Main.Size = UDim2.new(0, 0, 0, 0)
        Main.Visible = true
        local tweenIn = TweenService:Create(Main, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
            Size = UDim2.new(0, 240, 0, 360)
        })
        tweenIn:Play()
        tweenIn.Completed:Connect(function() isCoolDown = false end)
    end
end)

Notify("⚡ VOID VIP ⚡", "تم تحديث موقع البيك بنجاح وجاهز!")
